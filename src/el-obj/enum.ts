export enum LabelPosition {
  TOP = "top",
  LEFT = "left",
  RIGHT = "right",
  CENTER = "center",
}

export enum Size {
  SMALL = "small",
  DEFAULT = "default",
  LARGE = "large",
}

/** 输入框类型 */
export enum InputType {
  TEXT = "text",
  PASSWORD = "password",
  TEXTAREA = "textarea",
}

/** 组件分类 */
export enum ComponentType {
  INPUT = "input",
  SELECT = "select",
  RADIO = "radio",
  CHECKBOX = "checkbox",
}
